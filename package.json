{
  "name": "wave-defence",
  "private": true,
  "version": "0.4.0",
  "description": "코어 중심 그리드 방어 + 로그라이트 드래프트 웨이브 디펜스 (Phaser 4 · TypeScript · Vite)",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^4.2.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^8.1.0"
  }
}
